#include <stdio.h>

/* 
 * FEATURES:
 * - fast file streaming techniques
 * - quick parsing of text 
 * - graphics rendering (more specifically rendering nodes)
 *   - build up trie representation
 *   - render each node in a trie as a graphic
 *   - text rendering
 * - multithreading potentially?
 * - user input (keyboard, mouse)
 * - file exporter (jpeg, png, maybe svg)
 */

int main(void) {
    printf("hello, world!");
    return 0;
}
